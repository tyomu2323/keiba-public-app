# keiba-public-app v13

公開用の競馬分析ダッシュボードです。

## V13 追加内容

- 騎手詳細の距離成績を「芝」「ダート」に分離表示
- 芝距離別、ダート距離別、開催場別をそれぞれ確認可能
- 馬詳細に「基本 / 追い切り / 過去走」ジャンプボタンを追加
- 過去走の上がりを順位付きで表示
  - 1位：🥇金
  - 2位：🥈銀
  - 3位：🥉銅
- 詳細モーダルを閉じたら画面トップへ戻る
- 詳細を開くたびにモーダル内スクロール位置を先頭にリセット

## Render設定

Build Command:

```bash
npm install && npm rebuild better-sqlite3
```

Start Command:

```bash
node server/index.js
```

Environment Variables:

```text
ADMIN_USER=admin
ADMIN_PASSWORD=任意の管理者パスワード
JWT_SECRET=長いランダム文字列
NODE_ENV=production
```


## v14 追加内容
- 管理画面に「追い切り手動加点」を追加
  - 出走馬一覧と全追い切りを表示
  - タイム横のボタンで -3〜+5 を保存
  - 基準は0点
  - 追い切りは馬の状態・気持ち・動きを見るため、数字評価とは別に手動評価
- 最終表示を「自動加点 / 手動加点 / 総合点」に変更
- 自動加点ルールをユーザー理論に合わせて追加
  - 距離実績 +1
  - 同場所実績 +2
  - 右回り/左回り実績 +1
  - 小回りコース実績 +1
  - 大箱/外回り系コース実績 +1
  - 前走上がり3位以内 +1
  - 騎手直近1ヶ月勝率15%以上 +1
  - 重賞5着以内 +1
  - 前走距離延長/短縮で3着以内 +1
  - 同条件実績 +2
  - 自己条件レース +3
  - ハンデ戦は最重量を基準に1kg軽いごとに+0.5
- 距離延長/短縮が今回合うかどうかは手動判断扱いで、但し書き0点ルールとして表示


## V15 update
- 管理画面の追い切り一覧で枠/馬番をJRA配色に変更。
- 追い切り手動加点を馬単位ではなく調教タイムごとに保存するように変更。
- 加点保存時は画面全体を再描画せず、該当行と合計だけ更新して固まりにくくしました。
- レース詳細に負担斤量を追加。
- ハンデ戦斤量補正は「最重量 - 当該馬斤量」×0.5で自動計算します。


## V17 update
- 斤量補正をハンデ戦限定ではなく全レースに適用。
- 計算式: (レース内の最重量 - 当該馬の斤量) × 0.5。
- サンプルデータの斤量を 58, 57.5, 57, 56, 55.5, 55, 54, 52kg に分散。


## V18
- 斤量補正を明示的に 1kgごと+0.5 に固定。
- 表示理由に `差分kg × 0.5 = 加点` を出すようにしました。
- 計算式: `(レース内の最重量 - 当該馬の斤量) × 0.5`。


## V19 更新内容

- 管理画面の追い切り手動加点で、各ハロンごとに上位15%/25%を色分け表示
  - 馬なり25%: 黄
  - 馬なり15%: 赤
  - 強め/一杯25%: 水色
  - 強め/一杯15%: 青
- 管理画面の追い切り表でも自己ベストを🏆表示
- 回収率検証画面を追加
  - 追い切り手動加点+3以上
  - 総合点1位
  - 総合点2位
  - 総合点3位
  - 各項目で着度数（1-2-3-4着以下）、単勝回収率、複勝回収率を表示
- サンプルデータに検証用のレース結果を追加

※サンプルの複勝回収率は単勝オッズからの暫定推定です。JRA-VANの払戻データ接続後は実払戻で計算する想定です。


## V21変更点
- レース印は加点なしの目印として `entries.manual_mark` に保存します。
- 印登録はおすすめ馬と分離しました。
- おすすめ馬はPOG的な将来期待馬として、馬DB検索から `watch_horses` に登録します。
- 公開画面の「おすすめ馬・注目馬」は watch_horses を表示します。


## v22 追加機能
- 馬柱表示
- 人気・単勝オッズ・馬体重表示
- 同条件/同距離/同場所成績表示
- 前走着差表示
- 展開予想
- コース適性ランキング
- 馬詳細内に、今回騎乗騎手の距離/開催場/同条件成績を表示

差し替え後は `upgrade to v22` でCommit → Push → Render再デプロイしてください。


## v23 updates
- 管理者画面にログアウトボタンを追加。
- 適性点を新ルールに変更：同条件 1着5/2着4/3着3/4着1、同距離 1着2/2〜4着1、同場所 1〜3着1。
- 出馬表の加点表示を馬体重の横に移動。
- 馬柱クリックで過去レース詳細を表示。
- 展開予想を逃げ・先行・差し・追込の4分割にし、馬番を枠色付き丸数字で表示。
- 通過順と脚質から簡易ポジション予想を表示。
- コース適性ランキングと総合点ランキングを同時表示。


## V24 更新内容
- 詳細モーダルを閉じた時にホーム側をスクロールしないよう修正
- ポジションマップを削除
- 展開予想は逃げ/先行/差し/追込の4分割表示のみ残す
- 展開予想の馬番表示を出馬表と同じ枠色・馬番UIに統一


## v25
- 総合点ランキングを削除
- 適性ランキングと展開予想に整理
- 展開予想を2列カード化して横溢れを抑制
- 展開予想の馬番UIは出馬表と同じ枠色表示を維持
